---
版本：v1.0.0
最后更新：2026-08-25
适用课次：第 1 课
文档类型：课堂材料包
事实边界：以下契约、diff 样例与记录均为课堂构造或空白模板，不对应真实学生、账户或数据
变更记录：
- v1.0.0 (2026-08-25): 新建第 1 课课堂材料包：最小 prompt 可复制版、纸面任务契约、预期 diff 样例、日志空白与"未执行"口径、纸面验收三项（80→90+ 迭代轮 A 批，G8/G3/G2）
---

# 第 1 课课堂材料包

## 一、最小 prompt（可复制版）

【可替换处】换成你自己的内容；完整版与字段说明见讲义 §五。

**第一轮（只读，步骤 2）**

```text
读取 AGENTS.md 和 problem-definition.md 中的当前问题。

目标：提出三个范围收敛建议。
每条建议必须包含：建议修改的范围、依据、可能损失、需要研究者确认的问题。

限制：不修改任何文件；不访问项目以外目录；不联网；不把建议写成已证实事实。
```

**第二轮（受限写回，步骤 4）**

```text
根据我确认的第【N】条建议：
1. 只修改 problem-definition.md 的"当前问题"段；
2. 将原表述保留到"修改记录"；
3. 在 ai-usage-log.md 增加本次交互摘要；
4. 不修改其他文件，不访问网络。
完成后给出：修改摘要、实际修改的文件、仍需人工核验的内容。
```

## 二、纸面任务契约（模型不可用者使用）

在纸面或 Markdown 中写出：

- 第一轮预期：Agent 只读后可能给出的三条收敛建议（各含范围/依据/损失/待确认）；
- 人工取舍：采纳哪条、拒绝哪两条及理由；
- 第二轮预期写回：目标段落的新表述，以及保留到"修改记录"的原表述。

## 三、预期 diff 样例（标"预期非实际"）

```diff
# 预期 diff（模型不可用时的纸面替代；非实际执行结果）
--- a/problem-definition.md
+++ b/problem-definition.md
- 我想研究 LLM 在文献综述中的应用。
+ 在计算机领域文献综述中，LLM 辅助的证据追踪是否能降低主张—来源错配率？
```

纸面路径验收时，把本样例作为"预期 diff"提交，并在 AI 使用记录中按 §四 口径注明未执行。

## 四、AI 使用记录空白（表头以 starter-template §6 为准）

| 日期 | 工具 / 模型 | 用途 | 关键输入 | Context / 数据来源 | 输出用途 | 人工核验方式 | 问题或误导 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| | | | | | | | |

模型不可用口径：工具/模型栏写"未执行：模型不可用"，用途栏写"纸面完成任务契约与预期 diff"，人工核验方式栏写"教师/同伴纸面核对"；不伪造执行记录。未调用但完成人工判断时，写"本次为人工判断，无模型参与"并记录判断内容。

## 五、纸面验收三项（教师用）

- 任务契约含第一轮只读边界（纸面路径以"写出只读边界"替代"Agent 第一轮没写文件"）；
- 取舍有理由，写回限定目标段落且原表述保留；
- 预期 diff 与使用记录口径一致，标注"预期非实际"。

## 六、链路草图最小样例

见 [handout.md](./handout.md) §八·练习 5 的最小样例卡（8 行仅 2 行填不确定性，其余留空）。
